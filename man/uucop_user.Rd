% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_identity.R
\name{uucop_user}
\alias{uucop_user}
\title{Resolve the authenticated viewer's identity across hosting platforms}
\usage{
uucop_user(session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{session}{A Shiny session object.}
}
\value{
A character scalar; never \code{NULL}.
}
\description{
Tries, in order:
\enumerate{
\item \code{session$user} — populated on shinyapps.io and Posit Connect (server).
\item The \code{sub} claim of Connect Cloud's \code{Posit-Connect-User-Session-Token}.
\item \code{"unknown"}.
}
}
\details{
On Connect Cloud the returned value is an opaque account GUID rather than a
username. It is stable across sessions and apps, so it is suitable as a join
key, but it must be mapped to a roster entry to identify a student by name.
}
