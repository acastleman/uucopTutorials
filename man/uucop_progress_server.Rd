% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress.R
\name{uucop_progress_server}
\alias{uucop_progress_server}
\title{Show a live questions-answered counter in the tutorial header}
\usage{
uucop_progress_server(
  app_id,
  sheet_id = Sys.getenv("GS4_SHEET_ID"),
  min_questions = 20,
  min_minutes = 20,
  session = shiny::getDefaultReactiveDomain(),
  user = uucop_user(session)
)
}
\arguments{
\item{app_id}{This tutorial's id (the \code{TUTORIAL_ID} constant).}

\item{sheet_id}{Google Sheet ID. Defaults to \code{Sys.getenv("GS4_SHEET_ID")}.}

\item{min_questions}{Distinct questions required for completion.}

\item{min_minutes}{Cumulative minutes required for completion.}

\item{session}{A Shiny session object.}

\item{user}{The student to count for. Defaults to the authenticated viewer.
Override only to test the authenticated path locally, where \code{session$user}
is \code{NULL} and the header would otherwise stay in its "not signed in" state.}
}
\value{
Invisible \code{NULL}.
}
\description{
Call once from a \code{context="server"} chunk. The counter renders in the header
injected by \code{www/uucop-tutorial.js}, and only when that file's \code{uucop-config}
block carries a \code{progress} key -- so adding this call to a tutorial whose
config lacks one changes nothing on screen.
}
\details{
The client fires \code{uucop_progress_ready} once its header exists; that
handshake is what triggers the seed read, and it removes the race where the
server would otherwise push before the message handler is registered.
}
