% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autosave.R
\name{app_autosave_js}
\alias{app_autosave_js}
\title{JavaScript for client-side autosave of Shiny inputs}
\usage{
app_autosave_js(app_id, exclude = character(0), delay = 800, indicator = TRUE)
}
\arguments{
\item{app_id}{Identifier for this app, used as the storage key. Must be
unique per app (letters, numbers, \code{.}, \code{-}, \verb{_}) so that two apps open in
the same browser do not share a draft. Conventionally the same string
passed to \code{\link[=session_tracking_server]{session_tracking_server()}}, e.g. \code{"PKLab1"}.}

\item{exclude}{Character vector of input ids to leave out of the draft.}

\item{delay}{Debounce in milliseconds between the last keystroke and the
write to \code{localStorage}. Defaults to 800.}

\item{indicator}{Show the small "Draft saved" pill in the lower-right corner,
which also carries the "Clear draft" link. Defaults to \code{TRUE}.}
}
\value{
An \code{\link[htmltools:HTML]{htmltools::HTML()}} string to place in the UI.
}
\description{
Returns a self-contained \verb{<script>} + \verb{<style>} block that mirrors every
named input on the page into the browser's \code{localStorage} and restores them
the next time the same browser opens the app. Work survives a page refresh,
a shinyapps.io idle disconnect, and closing the tab for days.
}
\details{
Include it once anywhere in the app UI (top of \code{fluidPage()} is fine). No
server-side call is required for saving or restoring; pair it with
\code{\link[=app_autosave_clear]{app_autosave_clear()}} to discard the draft once a student has submitted.

Saved and restored: text, textarea, numeric, password, select (including
selectize), radio groups, checkboxes, checkbox groups, and date inputs.
Not saved: file inputs, action buttons, and any input without an id.

A draft is written only once at least one answer is non-empty, so simply
opening an app never creates one, and an accidentally blanked form never
overwrites a good draft with nothing.
}
\examples{
\dontrun{
ui <- fluidPage(
  app_autosave_js("PKLab1"),
  textInput("name", "Full Name"),
  actionButton("submit_btn", "Submit")
)

server <- function(input, output, session) {
  observeEvent(input$submit_btn, {
    # ... send the submission ...
    app_autosave_clear(session)
  })
}
}
}
