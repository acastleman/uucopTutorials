% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sheet_cache.R
\name{uucop_read_tabs}
\alias{uucop_read_tabs}
\title{Read Google Sheet tabs in one batched, cached request}
\usage{
uucop_read_tabs(sheet_id, tabs, ttl = 120)
}
\arguments{
\item{sheet_id}{Google Sheet ID.}

\item{tabs}{Character vector of tab names.}

\item{ttl}{Seconds a cached tab stays fresh. The default suits per-session
seeding, where staleness only matters across visits.}
}
\value{
Named list of data frames (all character columns) or \code{NULL}s.
}
\description{
Returns a named list, one element per tab (\code{NULL} if the tab is missing or
unreadable). Cached tabs cost nothing; the rest go out in a single
\code{values.batchGet}.
}
\details{
\code{batchGet} is all-or-nothing: one bad range 400s the whole request, which is
the normal case for a sheet that has never had, say, a \code{section_events} tab.
On failure this falls back to per-tab reads, which tolerate a missing tab at
a cost of 2 requests each — so keep course sheets fully provisioned.
}
