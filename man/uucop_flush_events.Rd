% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_buffer.R
\name{uucop_flush_events}
\alias{uucop_flush_events}
\title{Flush all buffered tutorial events to Google Sheets}
\usage{
uucop_flush_events()
}
\value{
Invisibly, the number of rows successfully written.
}
\description{
Writes every queued row, one \code{values.append} request per sheet tab. Safe to
call when the queue is empty (costs nothing). Called automatically by the
flush timer and by \code{\link[=session_tracking_server]{session_tracking_server()}} at session end; call it
directly only if you need an immediate write.
}
\details{
Rows that fail with a retryable status (429 or 5xx, after gargle's own
retries) are put back at the front of the queue for the next flush. Rows
that fail permanently are dropped with a message rather than retried
forever.
}
