% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autosave.R
\name{app_autosave_clear}
\alias{app_autosave_clear}
\title{Discard the saved autosave draft in the student's browser}
\usage{
app_autosave_clear(session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{session}{The Shiny session object.}
}
\value{
Invisible \code{NULL}.
}
\description{
Sends a message to the client telling it to delete the \code{localStorage} draft
written by \code{\link[=app_autosave_js]{app_autosave_js()}}. Call it after a submission has succeeded, so
that reopening the app gives a clean form rather than the answers the
student has already turned in.
}
\details{
Has no effect in an app that does not include \code{\link[=app_autosave_js]{app_autosave_js()}}.
}
