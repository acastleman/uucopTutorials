% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/question_events.R
\name{setup_question_recorder}
\alias{setup_question_recorder}
\title{Set up question event recording for learnr tutorials}
\usage{
setup_question_recorder(sheet_id = Sys.getenv("GS4_SHEET_ID"))
}
\arguments{
\item{sheet_id}{Google Sheet ID. Defaults to \code{Sys.getenv("GS4_SHEET_ID")}.}
}
\value{
Invisible \code{NULL}.
}
\description{
Installs a \code{tutorial.event_recorder} option that logs every question submission
to the \code{question_events} tab of the configured Google Sheet. Tracks attempt
number per user/question/day, captures answer text, and resolves the
authenticated user correctly on shinyapps.io.
}
\details{
Call this in a \code{context="server-start"} chunk, after \code{\link[=setup_gs4_auth]{setup_gs4_auth()}}.

\strong{User identity:} The \code{user_id} argument passed to the recorder by learnr
resolves to the OS user (\code{'shiny'}) on shinyapps.io. This function uses
\code{shiny::getDefaultReactiveDomain()$user} instead, with a fallback chain:
\code{domain$user} -> \code{user_id} (if not 'shiny') -> \code{"unknown"}.

\strong{Attempt counting:} Maintains a process-level environment keyed by
\code{user___question___YYYYMMDD}. Resets each calendar day.
}
